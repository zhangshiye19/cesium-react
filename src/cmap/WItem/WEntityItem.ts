import WItem from "./WItem";
import * as Cesium from 'cesium'

export default class WEntityItem extends WItem {

    constructor(position: Cesium.Cartesian3,props: object) {
        const options = {

        }
        super(position,props);
    }

}
